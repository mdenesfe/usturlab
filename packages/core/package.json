{
  "name": "@usturlab/core",
  "version": "0.1.0",
  "description": "Editor-agnostic engine of usturlab: rule-based routing, CLI adapters and quota failover across AI subscriptions.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:live": "USTURLAB_LIVE=1 vitest run test/live-brief.test.ts test/live-quality.test.ts"
  },
  "dependencies": {
    "minimatch": "^10.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
