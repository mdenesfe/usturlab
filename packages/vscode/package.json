{
  "name": "usturlab",
  "displayName": "usturlab — AI Subscription Router",
  "description": "Add all your AI subscriptions (multiple Claude accounts, Codex, Gemini, Copilot) and route every task to the best account and model with user-defined rules and automatic quota failover.",
  "version": "0.6.0",
  "publisher": "mdenesfe",
  "license": "MIT",
  "icon": "media/marketplace-icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/mdenesfe/usturlab"
  },
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "AI",
    "Chat",
    "Other"
  ],
  "keywords": [
    "ai",
    "router",
    "claude",
    "codex",
    "gemini",
    "copilot",
    "subscriptions"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "usturlab",
          "title": "usturlab",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "usturlab": [
        {
          "type": "webview",
          "id": "usturlab.chat",
          "name": "usturlab"
        }
      ]
    },
    "commands": [
      {
        "command": "usturlab.addAccount",
        "title": "usturlab: Add Account",
        "icon": "$(add)"
      },
      {
        "command": "usturlab.removeAccount",
        "title": "usturlab: Remove Account"
      },
      {
        "command": "usturlab.editRules",
        "title": "usturlab: Edit Routing Rules"
      },
      {
        "command": "usturlab.editCommands",
        "title": "usturlab: Edit Custom Slash Commands"
      },
      {
        "command": "usturlab.syncMcp",
        "title": "usturlab: Sync MCP Servers to All Profiles"
      },
      {
        "command": "usturlab.newConversation",
        "title": "usturlab: New Conversation",
        "icon": "$(clear-all)"
      },
      {
        "command": "usturlab.openChatInTab",
        "title": "usturlab: Open Chat",
        "icon": "$(comment-discussion)"
      },
      {
        "command": "usturlab.openAccounts",
        "title": "usturlab: Manage Accounts",
        "icon": "$(account)"
      },
      {
        "command": "usturlab.openRules",
        "title": "usturlab: Routing Rules",
        "icon": "$(list-tree)"
      },
      {
        "command": "usturlab.openInTerminal",
        "title": "usturlab: Open Session in Terminal"
      },
      {
        "command": "usturlab.cancelTask",
        "title": "usturlab: Cancel Running Task"
      },
      {
        "command": "usturlab.debug.simulateLimit",
        "title": "usturlab: Simulate Usage Limit (debug)"
      },
      {
        "command": "usturlab.openAnalytics",
        "title": "usturlab: Analytics",
        "icon": "$(graph)"
      }
    ],
    "menus": {
      "view/title": []
    },
    "jsonValidation": [
      {
        "fileMatch": [
          "**/.usturlab/rules.json",
          "**/rules.json"
        ],
        "url": "./schemas/rules.schema.json"
      }
    ],
    "configuration": {
      "title": "usturlab",
      "properties": {
        "usturlab.permissionMode": {
          "type": "string",
          "enum": [
            "safe",
            "edits",
            "full"
          ],
          "default": "safe",
          "markdownDescription": "How much autonomy routed CLIs get: `safe` (read/plan only), `edits` (auto-accept file edits), `full` (skip all approvals — use with care)."
        },
        "usturlab.pollUsage": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Proactively poll provider usage endpoints (Claude 5h/weekly window, Copilot AI credits) to show quota in the Accounts view. Uses undocumented endpoints; off by default."
        },
        "usturlab.cliPath.claude": {
          "type": "string",
          "default": "claude",
          "description": "Path to the claude CLI binary."
        },
        "usturlab.cliPath.codex": {
          "type": "string",
          "default": "codex",
          "description": "Path to the codex CLI binary."
        },
        "usturlab.cliPath.gemini": {
          "type": "string",
          "default": "gemini",
          "description": "Path to the gemini CLI binary."
        },
        "usturlab.cliPath.copilot": {
          "type": "string",
          "default": "copilot",
          "description": "Path to the copilot CLI binary."
        },
        "usturlab.notifyOnFinish": {
          "type": "boolean",
          "default": true,
          "description": "Show a notification when a chat task finishes while its tab is not focused."
        },
        "usturlab.midRunStrategy": {
          "type": "string",
          "enum": [
            "queue",
            "restart"
          ],
          "default": "queue",
          "markdownDescription": "What happens when you send a message while a task runs on a provider without live input (Codex/Gemini/Copilot). Claude always receives it live. `queue`: runs right after the current task. `restart`: cancels the in-flight task and re-asks with both messages merged — feels immediate, but discards unfinished tool work."
        },
        "usturlab.routingMode": {
          "type": "string",
          "enum": [
            "auto",
            "manual"
          ],
          "default": "auto",
          "markdownDescription": "`auto`: usturlab reads the task, judges its difficulty and picks the model — keeping nearly-exhausted accounts in reserve. `manual`: follow your rules and default chain exactly. Your rules always win in both modes."
        },
        "usturlab.autoPlanHeavyEdits": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "In auto mode, plan heavy code-writing work before it edits: usturlab switches that turn to Plan so you can review the approach. Turn off to let heavy work edit straight away."
        },
        "usturlab.sendWorkspaceContext": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Tell the model what it would have known sitting where you are: the open file and your selection, the branch and uncommitted changes, and the project's own convention files. Whatever a CLI already reads by itself is not sent twice."
        },
        "usturlab.standingInstructions": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Give each provider the standing instructions it needs to behave like the best one — delivered through its own system-prompt channel. Lines that measurably lower the clean-run rate are dropped automatically."
        },
        "usturlab.verifyChanges": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "After a run changes files, run the project's **own** typecheck/test script and, if it fails, give the model one chance to fix it. Commands are never invented — only what `package.json` or a `Makefile` declares. Never runs in Plan mode."
        },
        "usturlab.secondOpinion": {
          "type": "string",
          "enum": [
            "never",
            "hard",
            "always"
          ],
          "default": "hard",
          "markdownDescription": "Have a **different** provider review the work before you rely on it — a different model sees different mistakes. `hard`: only on work classified as hard. Costs a second account's quota, so it is skipped when headroom is low."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs && tsc -p tsconfig.json",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc -p tsconfig.json",
    "test:vscode": "vscode-test",
    "package": "node esbuild.mjs --production && vsce package --no-dependencies",
    "test": "vitest run"
  },
  "dependencies": {
    "@usturlab/core": "workspace:*",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/vscode": "^1.95.0",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.1.0",
    "@vscode/vsce": "^3.2.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9"
  }
}
