{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "usturlab routing rules",
  "type": "object",
  "required": ["version"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "version": { "const": 1 },
    "rules": {
      "type": "array",
      "items": { "$ref": "#/definitions/rule" }
    },
    "defaultChain": {
      "type": "array",
      "description": "Fallback chain tried in order when no rule matches (and appended after rule targets).",
      "items": { "$ref": "#/definitions/target" }
    }
  },
  "definitions": {
    "target": {
      "type": "object",
      "required": ["provider", "account"],
      "additionalProperties": false,
      "properties": {
        "provider": { "enum": ["claude", "codex", "gemini", "copilot"] },
        "account": { "type": "string", "minLength": 1, "description": "Account label as shown in the Accounts view." },
        "model": { "type": "string", "description": "Optional model override (e.g. sonnet, haiku, gemini-2.5-flash). Omit for codex — ChatGPT accounts only accept the CLI default." }
      }
    },
    "exclusion": {
      "type": "object",
      "required": ["provider"],
      "additionalProperties": false,
      "properties": {
        "provider": { "enum": ["claude", "codex", "gemini", "copilot"] },
        "account": { "type": "string", "minLength": 1, "description": "Bar only this account. Omit to bar the whole provider." }
      }
    },
    "rule": {
      "type": "object",
      "required": ["id", "match"],
      "anyOf": [
        { "required": ["target"] },
        { "required": ["exclude"] }
      ],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "description": { "type": "string" },
        "match": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": false,
          "description": "Fields present are AND-ed; values within a field are OR-ed.",
          "properties": {
            "keywords": { "type": "array", "items": { "type": "string" }, "description": "Case-insensitive substrings matched against the prompt." },
            "globs": { "type": "array", "items": { "type": "string" }, "description": "Glob patterns matched against the active file path." },
            "languages": { "type": "array", "items": { "type": "string" }, "description": "VS Code language ids (typescript, python, ...)." },
            "tags": { "type": "array", "items": { "type": "string" }, "description": "Tags selected in the composer." },
            "maxPromptChars": { "type": "integer", "minimum": 1, "description": "Rule only matches prompts up to this length." }
          }
        },
        "target": {
          "type": "array",
          "minItems": 1,
          "description": "Failover chain for this rule, tried in order. Optional when the rule only excludes.",
          "items": { "$ref": "#/definitions/target" }
        },
        "exclude": {
          "type": "array",
          "minItems": 1,
          "description": "Never route matching work here — applied to the whole chain, including the default chain. An explicit @mention still overrides it.",
          "items": { "$ref": "#/definitions/exclusion" }
        }
      }
    }
  }
}
