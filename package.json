{
  "name": "usturlab-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Route AI coding tasks across all your subscriptions (Claude, Codex, Gemini, Copilot) with user-defined rules and automatic quota failover.",
  "license": "MIT",
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "build": "pnpm -r build",
    "watch": "pnpm -C packages/vscode watch",
    "test": "pnpm -C packages/core test",
    "typecheck": "pnpm -r typecheck",
    "package": "pnpm -C packages/vscode package"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
